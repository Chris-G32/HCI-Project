<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--<Style x:Key="tmp" TargetType="{x:Type ComboBox}">
       


        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="125"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="Command" Value="{Binding PlayGame}"/>
        <Setter Property="FontSize" Value="14" />
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel>
                        <Image x:Name="button_play_image" Source="../../../Icons/button_play@2x.png"/>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <!--f-->
    <Style TargetType="{x:Type ComboBox}"
           x:Key="SearchBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}" x:Name="testing">
                    <Border CornerRadius="25"
                            Background="AliceBlue"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            >
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="White"/>
    </Style>
</ResourceDictionary>