<UserControl x:Class="HCI_Project.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCI_Project.MVVM.View"
             xmlns:viewModel="clr-namespace:HCI_Project.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Visibility="{Binding CanSee}">
    <UserControl.DataContext>
        <viewModel:SettingsViewModel/>
    </UserControl.DataContext>
    <Grid Background="{DynamicResource lnchDarkGray}">
        <StackPanel>
            <WrapPanel Margin="20,10,0,10">
                <TextBlock Foreground="White"  Text="Install Directory"/>
                <TextBox Margin="15,0,0,0" Width="300"/>
            </WrapPanel>
            <WrapPanel Margin="20,0,0,10">
                <TextBlock Foreground="White" Text="Another Setting"/>
                <TextBox Margin="15,0,0,0" Width="300"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
